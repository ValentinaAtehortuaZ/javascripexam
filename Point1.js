/*var plate = prompt(' Ingrese la placa del bus: ')
var passengers = prompt(' Ingrese le numero de pasajeros transportados: ')
var rute = prompt('Ingrese la ruta (A/B): ')
var total

if( rute == 'A' || rute == 'a' ){
    total = passengers * 1200
} else  {
    total = passengers * 1000
}
    
 alert( 'Hola'+ ' el dinero que recolectó es: ' + total)*/

